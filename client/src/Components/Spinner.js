import React from "react";

const Spinner = () => {
  return (
    <div class="ui active inverted dimmer" data-test="component-spinner">
      <div class="ui text loader">Loading</div>
    </div>
  );
};

export default Spinner;
